/**
 * TypeBadge - Prompt Type Display Component
 *
 * Displays the type of prompt (user, system, tool, eval, agent_instruction)
 * with appropriate icon and color coding. Used as a complication on PromptCard.
 */

export { TypeBadge, type TypeBadgeProps, type PromptType } from './TypeBadge';
